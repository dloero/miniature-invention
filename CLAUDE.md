# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty. It contains only a `README.md` with the project name (`miniature-invention`) and no source code, build configuration, dependency manifests, tests, or tooling.

There is no language, framework, build system, or test runner established yet. Any of these decisions are still open.

## Guidance for future sessions

- Before assuming a stack, re-check the repository — once real code lands, this file should be updated to document the actual build, test, lint, and run commands, plus the architecture.
- When the user asks for the first feature, confirm the intended language/framework rather than guessing from the repo name.
- Update this file as soon as conventions are established (package manager, entry points, directory layout, test command, CI), so subsequent sessions don't have to rediscover them.
